# Go
backend/bootstrap
backend/function.zip
backend/*.test

# Dependencies
backend/vendor/
# Frontend test harness only — the app itself ships as plain ES modules with
# no build step and no runtime dependencies. bun.lock IS committed so CI
# installs the same versions.
frontend/node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local config
frontend/js/config.js

# Logs
*.log

# Test coverage
coverage.out

#AI / editor tooling state
.claude/
.omc/
passbook-backup-*.json

# Local data backups (never commit — contain PIN hashes)
backups/
*.json.bak
