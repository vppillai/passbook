name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          cd backend
          python -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-mock moto

      - name: Run backend tests
        run: |
          cd backend
          source venv/bin/activate
          pytest tests/unit/ -v --tb=short

  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: 'backend/requirements.txt'
      
      - name: Cache SAM build
        uses: actions/cache@v3
        with:
          path: |
            backend/.aws-sam
            ~/.aws-sam
          key: ${{ runner.os }}-sam-${{ hashFiles('backend/infrastructure/complete-stack.yaml', 'backend/src/**/*.py') }}
          restore-keys: |
            ${{ runner.os }}-sam-

      - name: Install AWS SAM CLI
        run: |
          pip install aws-sam-cli

      - name: Install dependencies
        run: |
          cd backend
          python -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2

      - name: Create S3 deployment bucket if needed
        run: |
          BUCKET_NAME="passbook-sam-deployments-338186951935-us-west-2"
          if ! aws s3 ls "s3://$BUCKET_NAME" 2>&1 > /dev/null; then
            echo "Creating S3 bucket: $BUCKET_NAME"
            aws s3 mb "s3://$BUCKET_NAME" --region us-west-2 || echo "Bucket creation failed, may already exist"
          else
            echo "S3 bucket already exists: $BUCKET_NAME"
          fi

      - name: Deploy with SAM
        env:
          SMTP_USER: ${{ secrets.SMTP_USER || 'noreply@passbook.local' }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD || 'placeholder' }}
          SMTP_FROM: ${{ secrets.SMTP_FROM || 'noreply@passbook.local' }}
          SAM_CLI_TELEMETRY: 0
        run: |
          cd backend
          source venv/bin/activate
          chmod +x deploy-sam.sh
          ./deploy-sam.sh development us-west-2
